{
  "license": "1.0",
  "dependencies": {
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2"
  },
  "lint-staged": {
    "front/src/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "front/src/*.{css,less,sass,scss}": [
      "stylelint --fix"
    ]
  },
  "scripts": {
    "prepare": "husky install"
  }
}
